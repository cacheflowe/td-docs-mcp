[project]
name = "td-docs-mcp"
version = "0.1.0"
description = "MCP server providing TouchDesigner documentation to AI coding assistants"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "TD-DOCS-MCP Contributors" }
]
keywords = ["touchdesigner", "mcp", "documentation", "ai", "coding-assistant"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "mcp>=1.0.0",
    "crawl4ai>=0.4.0",
    "thefuzz>=0.22.0",
    "aiofiles>=24.0.0",
]

[project.scripts]
td-docs-mcp = "td_docs_mcp.server:main"
td-docs-mcp-crawl = "td_docs_mcp.crawler:main"
td-docs-mcp-clean = "td_docs_mcp.cleaner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/td_docs_mcp"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]
